import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

const WordCounter = () => {
  return (
    <div className="container mx-auto py-8">
      <Card>
        <CardHeader>
          <CardTitle>Word Counter</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Word Counter tool coming soon...</p>
        </CardContent>
      </Card>
    </div>
  )
}

export default WordCounter