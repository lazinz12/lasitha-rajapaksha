import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

const GradientMaker = () => {
  return (
    <div className="container mx-auto py-8">
      <Card>
        <CardHeader>
          <CardTitle>Gradient Maker</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Gradient Maker tool coming soon...</p>
        </CardContent>
      </Card>
    </div>
  )
}

export default GradientMaker